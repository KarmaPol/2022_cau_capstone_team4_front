import React from "react";
import { Box } from "@mui/material";

export default function Line() {
  return (
    <Box
      sx={{
        borderBottom: 0.5,
        borderColor: "grey.300",
      }}
    />
  );
}
