import React from "react";
import { Box } from "@mui/material";

export default function Line() {
  return (
    <Box
      sx={{
        width: "100%",
        borderBottom: 0.5,
        borderColor: "grey.300",
      }}
    />
  );
}
